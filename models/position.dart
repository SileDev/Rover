/// Modelo que representa una posición en un mapa o una posición de evaluación.

class Position {
  int x;
  int y;

  /// Constructor del modelo [Position].
  Position({
    required this.x,
    required this.y,
  });
}
